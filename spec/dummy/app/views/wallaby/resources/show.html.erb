<%
if resource
  decorated = decorate resource
%>
  <h1><%= show_title decorated %></h1>
  <% decorated.show_field_names.each do |field_name| %>
    <dl>
      <dt><%= decorated.show_label_of field_name %></dt>
      <dd><%= decorated.public_send field_name %></dd>
    </dl>
  <% end %>
<% end %>
